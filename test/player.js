var assert = require('assert');
var Engine = require('../dist/lib.js');
var utils = require('./utils.js');

var Player = Engine.__debug__.Player;

describe('Player', function() {
  it('', function () {
    
  });
});